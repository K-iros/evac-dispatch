# 项目背景与决策交接文档（PROJECT_CONTEXT）

> 本文档沉淀了前期调研与方案讨论的完整结论，供新对话/新成员快速对齐上下文。
> 项目根目录：`D:\File\waitan`
> 最后更新：2026-07-29

## 一、项目一句话定位

**面向社区帮扶者/网格员的"灾前疏散调度大脑"**：在洪水/台风/降雨型滑坡的预警窗口期内，为社区内的残障与弱势人群计算"最迟帮扶出发时间"，冗余匹配帮扶者，派发无障碍路线任务，并追踪到场与补位。

### 定位红线（产品措辞与 pitch 都必须遵守）
- ✅ 是"灾前规划 + 帮扶调度"工具，用户是**健全的帮扶者/社区管理者**，不是残障者本人
- ❌ 不是"灾中实时导航 App"——高危人群智能手机使用率低、灾中断网断电、GPS 误差对盲人致命
- ❌ 不承诺"保证救援"，措辞为"协助优先排序"（参考日本个别避难计划"尽力义务、不产生法律责任"的处理）
- ❌ 不覆盖地震（零预警，调度模式不成立），只做有预警窗口的灾种

## 二、idea 演进路径（为什么落到这个形态）

1. **初始**：地图沙盘 + 用户干预（植树/砍伐/修坝等）+ AI 实时灾害概率 → 调研发现有邻近项目（Tangible Landscape、CLIMADA、FloodSim 等）但无完整组合
2. **加 LLM agent 模拟人群灾害反应** → 传统 ABM 疏散模拟已 30 年历史（NetLogo / MATSim / Mesa / FLEE / RoboCup Rescue），LLM 驱动版很新但学术界已入场
3. **转向弱势群体疏散地图**：日常无障碍导航（有产品）× 灾害疏散（只有论文）= **交集处零产品**，空白真实
4. **增量扩展盲人画像**：可行，但约束模型与轮椅完全不同（见第五节）
5. **客观评估"能否降低遇难率"** → 灾中导航形态作用趋近于零；灾前 To G 形态可信 → **最终定型为社区调度工具**（当前定位，评估 ★★★★☆）

## 三、死因链条与产品作用点（应对评委质疑的标准答案）

东日本大地震残障者死亡率约为总人口 2 倍。事后研究的死因排序：

1. 没收到 / 没理解预警
2. **决定不走**（正常化偏误、"不想拖累别人"）← 头号死因
3. 想走但走不了（缺的是"来推轮椅的人"，不是路线信息）← 日本个别避难计划推行十年的卡点 = 人力
4. 电力依赖（呼吸机、电动轮椅断电）
5. 避难所本身不无障碍

### 本产品的作用机制
- **"最迟出发时间 14:30"这种具体截止时刻** → 对抗第 2 条的拖延与正常化偏误（行为学上远强于"请提前撤离"）
- **出发决策转移给帮扶者**（"14:20 前必须到张奶奶家"）→ 绕开残障者本人的 App 覆盖悖论
- **冗余匹配**（每个残障者 2-3 个备份帮扶者 + 实时到场追踪补位）→ 缓解第 3 条的到场率问题；工具不能创造善意，只能榨取既有人力的最大覆盖
- **批量计算"救助优先级清单"**（哪些户在任何灾害情景下都无自行撤离路线）→ 支撑灾前帮扶资源分配与基础设施改造决策

### 必须主动认领的天花板
- 纯信息工具的上限就是三到四星：残障疏散死亡根因是人力和社会结构问题
- 效果永远难以归因验证
- 依赖社区动员基础：一个社区没人报名帮扶者，工具无解
- 误报消耗志愿者意愿 → 产品需分级响应（预备 → 待命 → 出动），防"狼来了"

## 四、竞品格局（已核实，2026-07）

| 领域 | 现状 | 与本项目关系 |
|---|---|---|
| 日常无障碍导航 | ORS 轮椅 profile、AccessMap、Wheelmap、BlindSquare、Soundscape/VoiceVista、Lazarillo、GoodMaps | 成熟，直接复用不重造 |
| 灾害疏散 × 残障 | 只有论文和政策 PDF（Matsuo 2025、DTU 盲人火灾疏散实验、2026 MDPI 室内火灾 App 论文） | 论证需求真实性 |
| 日本个别避难计划数字化 | Pasona 防災ヘルプ、IDEA 要支援者システム、内阁府 2022 起试点 | 本质是"名册管理 + 地图可视化"，**无计算层** |
| 通用志愿者调度 | Zelos、PubSafe、Texas Disaster Volunteer Registry 等 | 无残障画像、无路径计算、无时间窗 |

**空白点 = 计算层：时变灾害风险 × 无障碍路径 × 最迟出发时间 × 帮扶者匹配。**
中国赛道接近空白，且有日本没有的部署载体——**社区网格员体系**（帮扶者不必依赖纯志愿者）。

## 五、技术方案

### 架构流水线

```
Geofabrik OSM(.pbf) + CV街景补无障碍标签(创新①)
        ↓
ORS Docker 自部署（wheelchair profile）──→ 各画像可行路径 + 耗时
        ↓
Copernicus DEM/FABDEM → 浴缸淹没模型(Python) → 每时刻淹没图层
        ↓
核心逻辑(创新②)：路径 × 时变淹没图层求交
  → 每段路的"失效时刻" → 倒推每户的"最迟出发时间"
        ↓
调度层(创新③)：带时间窗的帮扶者-残障者匹配（VRPTW 变体：
  志愿者位置 → 残障者家 → 避难所；推轮椅段必须走无障碍路径
  且必须在该路段失效前通过）
        ↓
前端 MapLibre/Leaflet：调度作战板
```

### 关键技术决策
- **淹没模型用浴缸模型**：`flooded = DEM < water_level(t)`，水位随时间线性上涨即可得到时变图层。PPT 诚实标注"MVP 采用简化静水模型，架构可插拔 HEC-RAS"。48 小时内不碰 HEC-RAS（要学建模、跑一次几小时）和 AI 代理模型（讲故事用）
- **ORS 必须 Docker 自部署**，不用公共 API——因为要向 OSM 数据注入 CV 识别的新标签后重建路由图
- **已知坑**：ORS 轮椅模式对无标签路段保守不走，OSM 标签稀疏地区常"找不到路径"；选演示城镇时先验证标签覆盖
- **DEM 数据**：Copernicus DEM 30m 或 FABDEM（去除建筑树木，更适合洪水），Python 用 `rasterio` 读取
- **OSM 获取**：小范围用 Overpass API，整城用 Geofabrik 下载 `.osm.pbf` 后裁剪

### 画像差异（轮椅 vs 盲人，约束模型完全不同）

| 维度 | 轮椅用户 | 盲人 |
|---|---|---|
| 核心约束 | 物理通行性 | 感知与信息获取 |
| 台阶/楼梯 | ❌ 硬性排除 | ✅ 可走 |
| 坡度/路面 | 核心约束 | 基本无关 |
| 复杂路口 | 无所谓 | ❌ 核心风险 |
| 关键 OSM 标签 | `wheelchair`、`incline`、`kerb`、`surface` | `tactile_paving`、`traffic_signals:sound`、`crossing` |
| 步速 | 慢 | 0.8-1.0 m/s（正常 1.4），噪声环境再打折 |

盲人画像实现方式：**不改 ORS 引擎**，用 walking profile 拿候选路径后**后处理打分**（盲道加分、声控信号灯加分、复杂路口重罚、转折次数罚分），约 1 天工作量。
LLM 语音路书（GeoJSON 路径 → 盲人友好自然语言 → TTS 播报）是最出彩的 AI 增量 demo。
**别碰硬件**（智能手杖/眼镜有成熟玩家）。

### 灾种选择与预警窗口
- 洪水/台风：1-3 天窗口，**最佳匹配，MVP 首选**
- 降雨型滑坡：几小时~1 天，可做但误报率高
- 地震：零预警，不做

## 六、Demo 主画面：调度作战板

- **左侧**：按"最迟出发时间"倒计时排序的待撤离名单（残障画像、地址、匹配状态）
- **右侧**：地图——志愿者与残障者的匹配连线、无障碍路线、实时淹没图层
- **顶部**：淹没推演时间轴（拖动可看任意时刻的淹没范围与路段失效状态）
- 加分演示环节：切换到盲人画像，播放 LLM 生成的语音路书（评委闭眼听 30 秒）

## 七、pitch 要点备忘

1. 开场用死亡数据：东日本大地震残障者死亡率 2 倍 + 日本个别避难计划十年完成率低的事实 → 需求无需论证
2. 主动说出天花板："我们不解决谁来推轮椅的问题，我们解决的是让帮扶资源在灾前就分配到对的人身上"——主动认领局限是加分项
3. 强调平台可扩展性：轮椅、盲人、老人、婴儿车……画像可插拔，证明是"产品"而不是"demo"
4. 技术诚实：浴缸模型 → 可插拔 HEC-RAS；架构里每一层都有工程级替换方案
5. 算法内核：调度层是带时间窗的任务分配 + 接送路径问题（VRPTW 变体），有真实运筹学含金量

## 八、下一步计划（骨架搭建顺序）

1. **项目初始化**：前端（React + TypeScript + Vite + Tailwind + MapLibre/Leaflet）+ 后端（Python：淹没模型 + 路径/匹配计算）
2. **数据准备**：选定演示城镇（先验证 OSM 无障碍标签覆盖率），下载 OSM `.pbf` 与 DEM
3. **核心算法层**：浴缸淹没模型 → 路段失效时刻 → 最迟出发时间 → 帮扶者匹配
4. **调度作战板前端** → 联调 → 盲人画像与 LLM 语音路书（增量，时间富余再做）

## 九、v1 功能决策（2026-07-28 讨论确认）

### 已确认的技术选型
- **路由引擎**：~~先用 ORS 公共 API（免费 key，2000 次/天）联调~~ → **已作废**，ORS 公共 API 在当前网络下被阻断（详见第十节验证结果）。现为 **Valhalla 公共实例**（`valhalla1.openstreetmap.de`，`pedestrian` costing + `type=wheelchair`）；赛前把 demo 场景路径预计算缓存到本地，断网可演示；自部署 Docker 作为后备方案
- **卫星底图**：Esri World Imagery（免 key、WGS-84 与 OSM 对齐），做成"街道/卫星"双模式切换按钮；禁用高德/腾讯瓦片（GCJ-02 偏移会导致路径错位）
- **"实时情况"的实现口径**：不承诺实时卫星影像（免费瓦片非实时），改为**淹没模型驱动的路段状态着色**：绿=畅通、黄=30分钟内将失效、红=已淹没，随时间轴联动

### v1 功能清单（按实施顺序，2026-07-29 全部完成）
1. ✅ **人物图标标记**：evacuees 图层 circle → symbol，按画像区分图标（轮椅/盲杖/拐杖），未匹配加红色角标，选中放大；Web 端用内联 SVG 运行时注册（mapIcons.ts）
2. ✅ **避难所醒目标识**：房屋图标 + 常驻名称标签 + 无障碍标识（弹窗展示 wheelchairAccessible）
3. ✅ **三段式路径 + 点击联动**：接人段（帮扶者→残障者家，琥珀虚线）/ 护送段（残障者家→避难所，粗实线），沿线方向箭头；点击名单只高亮该人主路径+备份路径，其余压暗；附左下角路径规划卡（画像策略/分段距离耗时）
4. ✅ **时间轴推演条**：顶部拖动条联动淹没图层 + 全局路段三态着色（绿=畅通 / 黄=30分钟内将失效 / 红=已淹没）+ 名单倒计时；路径卡同步受淹/预警告警
5. ✅ **补位模拟**：路径卡“演练”按钮触发主帮扶超时失联 → 备份自动升级为主，连线琥珀脉冲切换动画；前端派生态实现，顶栏可一键重置演练
6. ✅ **分级响应 + 数据看板**：standby/ready/dispatch 三档手动切换（出动档已匹配转在途）+ 已撤离/在途/未匹配/失联实时统计
7. ✅ **无路可走清单**：名单底部 P0（无任何路线）/P1（全部候选路线在推演窗口内失效）分级清单，可导出带 BOM 的 CSV 救助优先级清单；与补位演练实时联动

### 架构分工（待确认）
- Web 端 = 社区指挥端（调度作战板）
- 小程序 = 帮扶者接单端，MVP 只做"接单 + 到场打卡"两个动作

## 十、淹没模型升级与演示城镇更换（2026-07-28 确认，2026-07-29 ✅ 全部完成）

### 决策
- **演示城镇从上海外滩更换为有地形起伏的丘陵河谷小镇**（上海地形太平坦：DEM 垂直误差 1-2m 与洪水水深同量级，模型可信度和视觉层次都差）
- **淹没模型采用档位三：landlab OverlandFlow 简化水动力**（Bates 2010 惯性简化浅水方程），替换现有"水位线性上涨"浴缸模型

### 候选演示城镇（选定前必须先验证 OSM 标签覆盖）
| 候选 | 优势 | 风险 |
|---|---|---|
| 贵州榕江县城 | 2025 年真实特大洪灾地点，河谷+群山，叙事冲击力最强 | 县城 OSM 无障碍标签可能极稀疏，ORS 轮椅模式可能找不到路径 |
| 重庆磁器口/主城片区 | 地形起伏极端，OSM 覆盖较好 | 山城台阶极多，轮椅可行路径可能过少 |
| 广西阳朔县城 | 漓江河谷+喀斯特峰林，旅游区 OSM 覆盖好 | 无真实洪灾叙事背书（漓江有洪水记录可查） |

**验证步骤（选定前执行）**：用 Overpass API 统计候选区域的 `highway=footway/path`、`wheelchair=*`、建筑轮廓数量；用 ORS 公共 API 在候选区域试跑 3-5 条轮椅路径，确认能出结果。选定后更新 `MapView.tsx` 的 `INITIAL_CENTER/INITIAL_ZOOM` 与 `mock/data.ts`、`mock_data.py` 全部坐标。

### ✅ 城镇验证结果（2026-07-29 执行完成）

验证脚本：`scripts/verify_town_osm.py`（Overpass 标签统计）、`scripts/verify_town_valhalla.py`（轮椅路径试跑）、`scripts/verify_rongjiang_detail.py`（榕江覆盖复核）。原始数据：`scripts/town_verify_result.json`、`scripts/town_valhalla_result.json`。

#### 1. OSM 覆盖统计（bbox 约 8×8 km 核心建成区）

| 指标 | 榕江县城 | 重庆磁器口/沙坪坝 | 阳朔县城 |
|---|---|---|---|
| highway 总 way 数 | 326 | 3683 | 1096 |
| footway/path | 28 | 795 | 311 |
| steps 台阶 | 0 | 176 | 43 |
| wheelchair=* (way/node) | 0 / 0 | 3 / 10 | 2 / 3 |
| tactile_paving 盲道 | 0 | 0 | 0 |
| 建筑轮廓 | 35 | 1316 | 355 |

#### 2. 轮椅路径试跑（每镇 5 对 800-2500m 起终点，起终点自动取自真实路网节点）

| 城镇 | 轮椅成功率 | 步行成功率 | 轮椅平均绕行倍率 |
|---|---|---|---|
| 榕江县城 | 5/5 | 5/5 | 1.41 |
| 重庆磁器口/沙坪坝 | 5/5 | 5/5 | 1.48 |
| **阳朔县城** | **4/5** | 5/5 | **1.15（最优）** |

#### 3. 关键发现

- ⚠️ **ORS 公共 API 在当前网络下完全不可用**：`api.openrouteservice.org` DNS 被污染（解析到 Facebook/Twitter 段 IP），按真实 IP（129.206.5.53，HeiGIT）直连时 TLS 握手被 RST（SNI 阻断），无代理可用。**第九节"先用 ORS 公共 API 联调"的技术选型作废**，改用下述方案。
- ✅ **Valhalla 公共实例可直连**（`valhalla1.openstreetmap.de`）：其 `pedestrian` costing 原生支持 `type=wheelchair`（自动排除 steps、读取 `wheelchair`/`surface`/`incline` 标签），与 ORS wheelchair profile 目的等价，且响应 1-2s。**新的路由引擎主选方案**。
- ⚠️ **三地 `wheelchair=*` 标签均趋近于零，`tactile_paving` 全为 0**：印证第五节"OSM 标签稀疏地区"的已知坑。实测中 wheelchair 与 foot 返回的路径几乎完全相同（仅步速差异），说明**路由引擎当前无法真正区分无障碍路径**——这反向论证了创新①（CV 街景补无障碍标签）的必要性，同时意味着"无路可走清单（P0/P1）"必须由淹没模型 + 我方后处理打分产出，不能指望引擎自己给出。
- ❌ **榕江是真实的 OSM 数据荒漠**（已用 4×4 km 紧凑 bbox 复核，排除 bbox 偏移与 relation 型建筑的误判）：主城区仅 28 栋建筑、50 条 residential 道路、11 个命名 POI、0 个建筑 relation。DEM 条件化（建筑 +5m）几乎没有原料，地图视觉层次也撑不起来。

#### 4. 选镇决策：**广西阳朔县城**（备选：重庆磁器口/沙坪坝）

**选择理由**：
- OSM 覆盖足够且不过载（1096 条路 / 355 建筑 / 311 footway），DEM 条件化有原料，地图不会因要素过密而杂乱
- 台阶仅 43 条（重庆 176 条），轮椅可行路径不会被大量砍掉；实测轮椅平均绕行倍率 1.15 为三地最优，且有 1/5 用例出现"步行可走、轮椅无路"的真实分化——正是产品要暴露的问题
- 漓江河谷 + 喀斯特峰林，地形起伏满足淹没模型的垂直层次要求，且县城位于河漫滩，对 landlab OverlandFlow 的数值稳定性比重庆山城崖壁地形友好得多
- 规模是真正的"县城小镇"，与"社区网格员/县域指挥端"的产品定位吻合；重庆主城 8×8 km（3683 条路）尺度过大，landlab 网格与调度演示都会失焦
- 洪灾叙事有据可查：2024-06 桂林漓江遭遇 1998 年以来最大洪水，桂林水文站洪峰 148.88m、超警戒 2.88m、超 30 年一遇，阳朔位于漓江下游

**榕江的处理**：数据层出局，但**叙事层保留**——pitch 开场用 2025-06-24 榕江特大洪灾的硬数据（都柳江石灰厂站洪峰水位 256.71m、超保证水位 5.21m、流量 11400 m³/s，达特大洪水等级；6 人遇难、5 万余人紧急转移、一周内两次洪峰），再说明"以同类型河谷县城阳朔为演示场景"。这既拿到最强的现实动机，又不受 OSM 数据限制。

#### 5. 由此产生的方案修订

- **路由引擎主选改为 Valhalla 公共实例**（`pedestrian` + `type=wheelchair`），赛前把 demo 场景路径预计算缓存到本地，断网可演示；后备方案仍是 Docker 自部署（Valhalla 或 ORS 均可，Valhalla 镜像更轻）。第九节相应条目需同步更新。
- **`backend/app/core/routing.py` 需从 ORS 客户端骨架改造为 Valhalla 客户端**，`PROFILE_TO_ORS` 映射改为 Valhalla 的 `costing` + `costing_options.pedestrian.type`。
- **坐标迁移目标**：阳朔县城，`INITIAL_CENTER` 约 `[110.4900, 24.7780]`，bbox `(24.74, 110.45, 24.81, 110.53)`；需更新 `MapView.tsx` 的 `INITIAL_CENTER/INITIAL_ZOOM` 与 `mock/data.ts`、`mock_data.py`、小程序 `utils/mock.js` 全部坐标。
- **盲道标签为 0** → 盲人画像的后处理打分无法依赖 `tactile_paving`，改为以路口复杂度、转折次数、道路等级为主要打分因子，`tactile_paving` 作为存在即加分的可选项。

#### 6. ✅ 坐标迁移与 Valhalla 客户端改造（2026-07-29 执行完成）

- **`backend/app/core/routing.py` 已重写为 Valhalla 客户端**：`PROFILE_TO_COSTING`（wheelchair/blind/elderly → pedestrian type + walking_speed）、`DEPTH_THRESHOLD_M` 画像化水深阈值、`get_route`（支持帮扶者→住址→避难所多航点）、`get_matrix`（sources_to_targets）、`decode_polyline6`、`score_blind_route`（转折次数打分）。已用阳朔真实坐标联调验证（`scripts/test_valhalla_client.py`：route 1572m/2 legs、matrix 可达性正常）。
- **演示坐标已全部迁移到阳朔县城**，选点均为 OSM 真实地物（`scripts/explore_yangshuo_places.py` 探查）：避难所 = 阳朔公园（110.48455, 24.77903）+ 县实验小学；待撤离住址在漓江边低洼老城区（县前街/滨江路/桂花路）；进水点迁至漓江河道（110.4962, 24.7802）。涉及：`mock_data.py`、`data.ts`、小程序 `mock.js`、`MapView.tsx`（INITIAL_CENTER=[110.489, 24.779] zoom 15.2）、`flood.py`、小程序 `map.js`/`detail.js`。
- **三条 assignment 路线改为 Valhalla 实算折线**（`scripts/gen_yangshuo_demo_data.py` 生成，RDP 简化至 13-16 点，原始结果在 `scripts/yangshuo_demo_routes.json`），顶点仍包含帮扶者位置与住址，前端接人段/护送段拆分逻辑不变。
- 端到端验证通过：后端 `/api/schedule`、`/api/flood/frames` 返回阳朔数据；前端 tsc 无错，页面地图落在阳朔老城区且路线/标记/名单渲染正常。


### 档位三实施方案（landlab OverlandFlow）
- **依赖**：`pip install landlab`，后端 `services/` 下新增 `flood_sim.py`
- **输入**：FABDEM 裸地面 DEM（裁剪演示城镇范围）+ DEM 条件化（OSM 建筑轮廓 +5m、道路 −0.3m，`rasterio` 栅格化叠加）+ 入流边界（河道上游断面给流量过程线）或降雨源项
- **运行方式**：**离线预计算**，不在演示时实时跑——模拟一场洪水输出每 5-10 分钟一帧的水深场，转成 `FloodFrame` 序列存 JSON/GeoJSON，前端接口完全不变
- **路段失效判定升级**：由“是否被淹”改为**按画像分水深阈值**——轮椅 ≥0.15m 失效、步行 ≥0.3m 失效、车辆 ≥0.5m 失效；每路段取沿线格子最大水深判定
- **已知风险与对策**：数值稳定性需调时间步长（landlab 有自适应步长选项）；若调参卡壳超过 1.5 天，**回退档位一+二组合**（连通性浴缸 h* 场 + DEM 条件化），对外接口一致，随时可切
- **PPT 口径**：“采用惯性简化浅水方程（Bates 2010）离线推演 + 实景条件化 DEM，架构可插拔 HEC-RAS 工程级内核”

#### 7. ✅ landlab 淹没推演落地（2026-07-29 执行完成，第十节全部完成）

- **数据准备**：`scripts/fetch_yangshuo_dem.py` 下载 Copernicus GLO-30（AWS COG `/vsicurl/` 窗口读取，免整幅下载；FABDEM 需注册不可行，垂直精度差异对演示可接受），输出 `backend/data/yangshuo_dem_utm.tif`（EPSG:32649、20m、355×331、高程 102～417m）；`scripts/fetch_yangshuo_osm.py` 下载建筑/道路/水体（281 建筑 / 857 道路 / 46 水面 / 5 河道中心线 → `backend/data/yangshuo_osm.json`）
- **DEM 条件化 + 推演**：`scripts/run_flood_sim.py`（未按原计划放 services/，因为是离线脚本而非运行时依赖）——建筑 +5m / 道路 −0.3m 栅格化叠加；水位边界取漓江主河道栅格（面积 ≥0.1km² 的宏观水体，排除山塘），每单元以自身 DSM 水面高程为基准按 0.8 m/h 抬升（与 `water_level_at` 曲线一致），满溢后由 OverlandFlow（steep_slopes、mannings_n=0.04）向两岸漫流；180 分钟推演实测 53s 跑完，无数值稳定性问题，**未触发回退预案**
- **FloodFrame 导出**：每 10 分钟一帧共 19 帧，每帧水深场按 **0.05 / 0.15 / 0.30 m** 三档阈值矢量化为 WGS84 多边形（`properties.minDepth`）存 `backend/data/flood_frames.json`（1.9MB）；淹没范围从 0 渐进扩张到 4.55 km²，沿漓江河谷分布，物理形态合理
- **flood.py 接入**：`compute_flood_frames` 优先加载预计算 JSON（lru_cache），缺失时回退浴缸演示帧（已同构改为含 minDepth 的嵌套矩形，接口一致）
- **路段失效判定已升级为画像化水深阈值**：前端 `routeUtils.ts` 新增 `DEPTH_THRESHOLD`（轮椅 0.15 / 盲人・老人 0.30，与后端 `routing.DEPTH_THRESHOLD_M` 对齐），`pointInFlood/segmentStatus/statusSegments/buildRoutePlan/routeFailureMinute/buildPriorityList` 全链路按阈值过滤深度等级面；`MapView.tsx` 淹没图层按水深分级着色（浅蓝/蓝/深蓝 step 表达式），路线逐段按各自画像阈值着色；小程序 `map.js` 同步分色，mock 三端同格式
- **端到端验证通过**（tsc 零错 + API 19 帧 + 浏览器截图）：滨江路住户（盲人陈先生）路线在 +130 分钟达到 ≥0.30m 阈值失效 → 自动进入 P1 清单；高处点位（阳朔公园避难所等）全程安全，“沿江先失效”叙事闭环成立

### 工作量估算（已全部完成）
- ✅ 城镇验证（Overpass 统计 + 轮椅路径试跑）：已完成
- ✅ 坐标迁移到阳朔 + 路由层改造为 Valhalla 客户端：已完成
- ✅ 数据准备（DEM 裁剪 + OSM 下载）：已完成
- ✅ DEM 条件化（建筑 +5m / 道路 −0.3m）：已完成
- ✅ landlab 模拟调通 + FloodFrame 导出 + 画像化水深阈值失效判定：已完成（未触发回退预案）

## 十一、第十节扩展：水动力数据消费功能 + 淹没模型修正（2026-07-29 讨论确认，2026-07-29 ✅ 全部实施完成）

### 必做前置（第十节隐含验收标准）
**最迟出发时间真实化**：`latestDeparture` 当前仍是 mock 值，必须改由真实水深场反推——landlab 水深场 → 路段失效时刻 → 倒推每户最迟出发时间。否则会出现"时间轴上路已变红、名单倒计时却仍充裕"的自相矛盾。此项是 A-D 四项功能的共同数据基础。

### 扩展功能清单（A/B/C/D 全部确认，按实施顺序）
| 序 | 功能 | 要点 | 依赖 | 工作量 |
|---|---|---|---|---|
| 1 | **A. 水深着色 + 播放模式** | 淹没区按水深渐变着色（0.15m 浅蓝→2m+ 深蓝）；时间轴加 ▶ 播放按钮自动推进 | FloodFrame 增加 depth 字段 | 0.5 天 |
| 2 | **B. v×d 危险度判据** | 行人失稳判据 `水深×流速>0.5 m²/s`，路段失效=水深阈值 **或** v·d 超限；轮椅阈值更严。流速场是水动力独有数据，是选档位三的最有力证明 | landlab 流速场输出 | 0.5 天 |
| 3 | **C. 情景预设** | 下拉切换「30年一遇」「2024-06 漓江洪水情景」「极端情景」；措辞用"情景"不用"复现"（阳朔断面无完整流量过程线，按洪峰水位反推） | 模拟管线多跑 2-3 组过程线 | 0.5 天 |
| 4 | **D. 避难所失效校验 + 自动重分配** | 避难所被淹或 v·d 超限时标红叉，分配到该所的人自动改派次近可达避难所；与补位模拟呼应"双类失效自愈" | B 的失效判定 + 现有匹配逻辑 | 1 天 |

**接口要求**：`FloodFrame` 一次改到位——geojson feature 携带 `depth`、`vd` 属性（或另存栅格采样值），A/B/D 三项共用，避免返工。

### 双模式推进交互（时间轴）
- **拖动模式**（已完成）与**自动播放**（功能 A）共存
- 交互约定：▶ 播放时匀速推进帧序列；**用户一拖动即自动暂停**，从拖到的位置可继续播放；播放速度 1x / 4x 两档（讲解用慢速、过场用 4x）
- 不新增工作量，属功能 A 实施细节

### 淹没模型修正一：水位基准与时间尺度
**已发现的基准错误**：模拟的 2.4m 是从常水位（DEM 河道现状水面）起涨的涨幅，而新闻里的"超警 1.27m/2.88m"是相对警戒水位的幅度——两者量纲不同。警戒水位本身比常水位高数米（桂林站常水位约 142-143m、警戒 146m、2024 洪峰 148.88m，**从常水位到洪峰真实总涨幅约 6m**）。当前 2.4m 只淹河岸低漫滩是物理上正确的结果——水还没漫堤。

修正措施：
- **总涨幅 2.4m → 约 5.5m**（终点对应超警约 1.5m，介于阳朔 1.27m 与桂林 2.88m 之间）
- **时间尺度 3h → 24h**（1440 分钟）：3 小时淹城与第五节"洪水预警窗口 1-3 天"的产品前提自相矛盾。帧间隔 10 分钟 → 60 分钟，帧数基本不变
- **涨速改为分段过程线**：前 12h 缓涨（~0.05 m/h）→ 急涨段 8h（~0.2-0.5 m/h）→ 临峰趋缓（参照桂林站 2024-06-18 首峰 147.55m → 06-19 急涨 → 06-20 00:55 主峰 148.88m 的节奏）
- **前端时间轴标签**："+N 分钟" → 模拟时钟时刻（如"6月19日 17:00"），与名单"最迟出发时间 14:30"同一坐标系
- **顶栏水位显示**："水位 2.4m" → "阳朔站 110.9m（超警 +1.4m）"绝对水位+超警幅度表达，与新闻报道同话语体系

### 淹没模型修正二：补齐内涝水路（pluvial）
2024 大面积城市积水 = 河道漫溢（fluvial，已有）+ **降雨就地积水（pluvial，缺失）** 双机制叠加。修正：
1. **加降雨源项**：OverlandFlow 支持逐步长降雨强度输入；按 2024 量级给雨型（累计 200-300mm/24h，峰值时段 40-60mm/h），全域落雨、低洼处自然汇积
2. **加排水能力负源项**（防反向失真）：每步长扣减固定排水速率（县城 15-25mm/h），暴雨超出部分才积水；**外江水位超过管口高程时排水速率置零**——复现"顶托失效"机制
3. **定性验证**：跑完对照 2024-06 实况——阳朔西街、县前街一带应现积水；淹没形态应从"沿河一条带"变为"河谷低洼面状 + 沿江深水带"复合格局

### 工作量汇总
- 淹没模型修正（过程线 + 降雨/排水源项 + 重跑）：约 0.5-1 天（24h 时长重跑估计 5-10 分钟）
- A+B+C+D 四项功能：约 2.5 天（C 与模型修正共用过程线工作）
- 前端标签/水位显示改造：随功能 A 一并完成
- 实施顺序：模型修正 → 最迟出发时间真实化 → A → B → C → D

### 实施结果（2026-07-29 完成，端到端验证通过）
- **模型修正**：`scripts/run_flood_sim.py` 重写为三情景批量管线（24h/1440min、帧间隔 60min、分段过程线 + 降雨源项 200-300mm/24h + 排水负源项 20mm/h（外江顶托后置零）），产出 `backend/data/flood_frames_{s30,s2024,extreme}.json`（25 帧，8.9/11.0/12.2 MB，峰值涨幅 4.2/5.5/7.0m）。水位口径：BASE_STAGE=105.5m、WARN_STAGE=109.5m、SIM_T0=2024-06-19T00:00，s2024 末帧 111.0m（超警 +1.5m）。每帧 feature 携带 `minDepth`（六档 0.05-2.00m）或 `minVd`（三档 0.25/0.40/0.50 m²/s）+ 帧级 `stageM/warnM/clock`，接口一次改到位。
- **最迟出发时间真实化**：`backend/app/core/departure.py` 沿路径反向递推（段失效时刻−行进耗时，步速接人 1.4 m/s、护送按画像 0.7-0.9 m/s）；s2024 反推结果：e-1 14:48、e-2 02:55（滨江路内涝水路 +360min 先淹，最紧迫）、e-3 20:30。
- **A**：六档水深渐变（MapLibre step 表达式）+ ▶ 播放（1x=1.5s/帧、4x，拖动即暂停，末帧自停）+ 时间轴模拟时钟标签 + 顶栏「阳朔站 xxx.x m（超警/低于警戒）」。
- **B**：`routeUtils.ts` 全面改为 `HazardThreshold{depth,vd}` 双判据（路段失效 = 水深超阈 或 v·d 超限；轮椅 0.15m/0.25、盲人 0.30m/0.50、老人 0.30m/0.40），地图叠加橙红 v·d 急流危险面图层。
- **C**：`GET /api/scenarios` + `/api/schedule|/api/flood/frames?scenario=` 参数；顶栏情景下拉切换，切换后回 T0 并重拉两接口（stale 标记防竞态）。
- **D**：新增临江低洼避难点 sh-3「山水园临江避难点」(110.4923, 24.7791)，e-2 默认派往；避难所失效判据 depth≥0.3m 或 v·d≥0.5；失效时红底白叉图标 + 前端自动升级 `isFallback` 改派候选（h-2→e-2→sh-1）为主路线并展示「已自动改派」横幅。s2024 演示时序：sh-3 前端判定 +1020min 失效、sh-1 +1260min，改派窗口帧 17-20；+1260 后两所均失效则不改派（合理兼平）。选址教训：首版 sh-3 与 sh-1 同帧失效导致改派永不触发，已用水深场扫描重选位置。
- **小程序同步**：mock/倒计时/时钟标签/六档填色/v·d 面/阳朔站水位与大屏端对齐（utils/mock.js、format.js、pages/map、pages/detail）。
- **验证**：`npx tsc -b --noEmit` 零错误；`scripts/smoke_verify_s2024.py`（保留作回归）全部通过；浏览器端到端 A/B/C/D 逐项验证通过（截图 e2e_step*.png、reverify_frame18_reassign.png），控制台无应用错误（仅 openmaptiles 字体源超时，已本地回退）。

## 十二、项目打磨优化计划（2026-07-29 脑暴确认，含 P0/P1/P2 分级）

### 背景：评委视角的三个硬伤
1. **AI 含量太低**：landlab 是物理模型、Valhalla 是路由、VRPTW 是运筹学——严格说 AI 为零，两个 AI 创新点（CV 补标签、LLM 语音路书）均未落地
2. **核心卖点"无障碍"不可见**：阳朔 `wheelchair` 标签为 0，轮椅与步行路径几乎相同，demo 里看不出"考虑残障"的视觉证据
3. **demo 规模 n=3 撑不起"调度大脑"**：3 户 / 3 帮扶者 / 避难所 1-2 个，优先级排序、看板统计、避难所改派均无从展示

### AI 增量（3 项全做，约 3 天）
| 项 | 内容 | 工作量 |
|---|---|---|
| LLM 派单简报 ✅（2026-07-29 完成） | 每个帮扶者一句话任务卡（接谁/几点前到/走哪条路/注意什么），后端一次 API 调用 | 0.5 天 |
| LLM 语音路书 ✅（2026-07-29 完成） | 盲人画像 GeoJSON 路径 → 自然语言路书 → TTS 播报，demo"评委闭眼听 30 秒"环节 | 1 天 |
| CV 补标签最小闭环 | 3-5 张阳朔街景 → 视觉模型识别台阶/坡道 → 注入 OSM 标签 → 同一起终点路径改变 | 1-1.5 天 |

### 算法与数据层（4 项全做，约 2.5 天）
- ✅ **扩充数据规模**（2026-07-29 完成）：24 户 / 10 帮扶者 / 4 避难所（新增 sh-4 县外语实验中学安置点），坐标均取自 OSM 真实地物——14 户漓江边低洼老城区（滨江路/县前街/桂花路/西街）+ 10 户西侧高地对照组。实施要点：
  - 生成管线 `scripts/gen_scale_dataset.py`：距离贪心+容量上限预匹配（真实时间窗匹配留待 P1）→ Valhalla 实算 28 条三点航线（全部成功）→ RDP 简化 → 复用后端 `build_mock_schedule("s2024")` 序列化前端回退 JSON，离线/在线逐字段一致
  - 产物：`backend/data/yangshuo_schedule.json`（后端运行时加载，`mock_data.py` 已从硬编码重构为读数据集）+ `frontend/src/mock/scheduleDataset.json`（前端 mock 直接 import，tsconfig 开 resolveJsonModule）
  - 剧情保留：4 户未匹配（e-3/e-11/e-23/e-24 撑 P0 清单）、6 户带备份帮扶（补位演练）、e-2/e-4 派往小容量 sh-3 且各带 fallback 路线（功能 D 双改派，18:00 帧起触发，已浏览器验证）
  - 扩容性能加固：路线失效时刻两级 WeakMap 缓存 + 淹没面 bbox 预筛（routeUtils.ts）、failedShelterIds 引用稳定化（App.tsx）——25 帧连拖实测均值 123ms/帧；后端 lifespan 后台预热三情景（冷启动单情景反推 15-30s → 预热后切情景 <0.1s）
- ✅ **真实匹配算法**（2026-07-29 完成）：贪心 + 时间窗校验替换硬编码预匹配，含 7 条"一人串行接多户"链。实施要点：
  - 核心 `backend/app/core/matching.py`：`match_helpers` 贪心按 deadline 紧迫度升序逐户指派——首单校验"接人耗时 ≤ deadline"，串行第 2+ 单做**最坏情况链校验**（前单 deadline + 护送耗时 + 避难所→下户转移耗时 ≤ 本单 deadline，保证前单拖到最迟也赶得上）；避难所按容量 + 轮椅无障碍兼容选择；`locked` 人工锁单只校验告警不改派、`with_backup` 冗余备份；贪心升序使链内 deadline 单调不减，自然涌现"先救紧迫低洼户、再接宽松高地户"画面
  - 管线 `scripts/gen_match_dataset.py`（两阶段决策）：deadline = `point_flooded_minute`(s2024) − 30min 缓冲 → Valhalla `sources_to_targets` 画像化耗时矩阵（接人/转移 foot 1.4m/s、护送按画像折减）→ 求解 → 路线定案后 Valhalla 实算（串行第 2+ 单起点 = 前一单避难所，"送达后再出发"）→ 回写 `yangshuo_schedule.json` + 前端回退 JSON。结果：20 户全匹配、7 条串行链（h-2: e-2→e-8→e-21 等）、0 落单；叙事锁单（e-1/e-2/e-4）与 4 户剧情未匹配户（EXCLUDE）保留
  - 透传与文案：`Assignment.sequence`（≥2 即串行）后端→前端全链路；路径卡紫色角标「⛓ 串行任务 第 N/M 单 · 送达上一户后从避难所出发」；简报三处口径统一——串行第 2+ 单用「送达后转接」不报独立时刻（各单最迟时刻是独立约束上限，逐单报时会倒挂）
  - 绕行案例重建：`gen_access_compare.py` 升级支持串行起点 + 迭代堵路（逐轮把当前轮椅路线护送段中点加入 exclude，最多 4 轮），重跑结果 e-8 x1.65 / e-14 x1.22 / e-19 x2.21
- ✅ **无障碍路径对比开关**（2026-07-29 完成）：人工构造 3 个轮椅/步行分化案例 + 同屏双线对比，与 CV 补标签串成"识别→注入→路径改变"闭环。实施要点：
  - 数据管线 `scripts/gen_access_compare.py`：选 3 个无备份路线的轮椅户（e-8 卢阿姨西街 / e-14 秦阿婆桂花路 / e-19 欧奶奶画山路），在步行路线护送段按行程占比逐点尝试障碍位置（叙事：CV 街景识别台阶/陡坎），用 Valhalla `exclude_locations` 实算轮椅绕行路线直至绕行倍率 ≥1.06；主路线替换为绕行版，同航点 `type=foot` 实算对照线存 `footRoute`。结果：e-8 x1.07 / e-14 x1.67（主案例，2.119→3.548km）/ e-19 x1.09（P1 真实匹配算法落地后已用升级版管线重建，最新倍率见上方"真实匹配算法"条目）
  - 后端：`Assignment.foot_route` + `ScheduleState.access_cases`（`AccessCase`：障碍点坐标/标签/双里程/绕行倍率）透传；前端：MapView 左上「♿ 无障碍对比」开关 → 灰虚线步行对照线（foot-routes-line）+ ⚠️ 障碍标记（access-barriers-symbol，点击气泡显示绕行倍率）+ 对比卡片（3 案例双里程/倍率，点击定位），已浏览器验证
- **时间倒推链可解释**：点击最迟出发时间展开推导链条（路段 X 于 15:10 失效 → 护送 25 分钟 → 接人 15 分钟 → 最迟 14:30 出发），0.5 天

### 小程序处理（已决策：聚焦 Web 端）
- 本阶段不再投入小程序开发，PPT 中作为架构图展示
- **留接口**：后端保留/补齐 `POST /assignments/{id}/accept`、`POST /assignments/{id}/checkin` 占位端点 + `Assignment` 状态机字段，未来"Web 派单 → 手机接单打卡 → Web 状态变化"流程即插即用

### 演示可靠性（赛前最后收尾，约 1 天）
- **断网预案**：Esri 瓦片预热缓存（演示 zoom 范围）+ 完整流程录屏兜底；路径已预计算缓存
- **数据源指示灯**：`tryFetch` 失败会静默回退前端 mock，台上察觉不到后端未启动——加角落指示灯（🟢 后端 / 🟡 本地）
- **一键演示剧本模式**：自动编排"预警升级 → 时间轴播放 → 点击张奶奶 → 触发补位 → 导出清单"序列
- 投影适配：路段红/绿三态加虚线纹理区分（色弱友好）

### P0/P1/P2 分级（总量约 10 个工作日，时间紧张时按此取舍）
- **P0 砍无可砍（约 4 天）**：淹没模型修正（十一节）✅、最迟出发时间真实化 ✅、A 水深着色+播放 ✅、扩数据规模 ✅、无障碍对比开关 ✅、LLM 派单简报 ✅ —— **P0 全部完成（2026-07-29）**。LLM 简报实施要点：
  - 后端 `app/services/briefing.py` + `GET /api/briefings?scenario=`：按帮扶者汇总任务事实（主派按最迟出发排序 + 备份职责 + 画像注意事项，对比案例户注「街景识别台阶障碍绕行」），有 `DEEPSEEK_API_KEY` 环境变量时一次 DeepSeek 调用（deepseek-chat，json_object 输出，结果按情景缓存，漏答者模板补齐）生成全部；无 Key/失败时规则模板兜底，`source: llm|template` 标注来源
  - 前端：TopBar「🤖 AI 简报」按钮 → BriefingPanel 右侧抽屉（来源徽章 + 每帮扶者任务卡）；后端离线时前端用同口径本地模板兜底，演示永不空白。已验证：`/api/briefings` 返回 9 张任务卡（模板源，见 scripts/briefings_s2024.txt），浏览器抽屉正常渲染、无控制台报错
- **P1 强烈建议（约 3.5 天）**：B v×d 危险度判据 ✅（随十一节完成）、真实匹配算法 ✅、LLM 语音路书 ✅、C 情景预设 ✅（随十一节完成）—— **P1 全部完成（2026-07-29）**。LLM 语音路书实施要点：
  - 后端 `app/services/roadbook.py` + `GET /api/roadbook?evacueeId=&scenario=`：事实提取纯几何+水深场（主路线护送段折线 → 方位角/转角序列 → 转向步骤清单「沿东南方向出发，直行约 120 米」「左转，继续直行约 80 米」；逐小段 `segment_failure_minute` 最早失效时刻 → 水情提醒——水动力推演独有信息，通用导航给不了）；有 `DEEPSEEK_API_KEY` 时一次 DeepSeek 调用润色成视障陪同导引口语路书（150-220 字约 30 秒），无 Key/失败模板拼接兜底（`source: llm|template`，LLM 结果按 (scenario, evacueeId) 缓存）；无主路线 404
  - 前端 `RoadbookPanel.tsx`：盲人户路径卡「🦯 语音路书」入口（叙事最强，接口对任何有主路线户可用）→ 面板展示整段路书 + 转向步骤 + 来源徽章，`speechSynthesis` TTS 播报/停止（zh-CN, rate 0.95，关闭/换户自动 cancel）；后端离线时前端按同口径本地模板兜底（含转向步骤本地几何计算），演示永不空白
  - 已验证：`npx tsc -b --noEmit` 零错误；API——20 户主派 / 7 条串行链 sequence 正确 / h-2 简报「送达后转接」口径 / e-2 路书 198 字含 06:00 水情提醒 / e-3 未匹配 404；浏览器 DOM 断言——e-2 串行角标「第 1/3 单」+ 路书面板打开（规则模板徽章）+ TTS speaking→停止状态切换正常，e-8 角标「第 2/3 单 · 送达上一户后从避难所出发」且非盲人户无路书入口
- **P2 富余再做（约 3 天）**：D 避难所失效重分配 ✅（随十一节完成）、CV 补标签最小闭环、倒推链可视化、演示剧本模式

只完成 P0 时 demo 已闭环且 AI 含量达标；P1 补齐算法含金量与高光演示；P2 为锦上添花。

## 十三、使用反馈优化批次（2026-07-29 定稿，2026-07-30 全部 9 项实施完成 ✅）

> 来源：用户实际使用作战板后反馈 8 个问题，逐条对照代码与数据诊断后定稿。
> 诊断依据：`matching.py` 贪心逻辑、`gen_match_dataset.py` 两阶段管线、s2024 水深场实测
> （各户住址淹没时刻 / 路线反推最迟出发 / 三情景避难所失效时刻）。

### 诊断结论备忘（回答"为什么"的部分，非改动项）

- **⑤ 李网格送卢阿姨"路过山水园却去县外语实验中学"——算法行为正确，是信息未透出**：
  e-8 是 h-2 串行链（e-2→e-8→e-21）第 2 单，路线起点 = 前一单避难所 sh-3（"送达后再出发"），
  山水园是出发地不是绕路；sh-3 容量 2 已被锁单 e-2/e-4 占满，`pick_shelter` 在余量+轮椅可达集合
  {sh-1, sh-4} 中选护送耗时最近的 sh-4。修复方向见下表第 5 项（容量透出，不改算法）。
- **⑥ 卢阿姨名单时间最少（03:35）却排第二单——两套时间口径不一致，真实矛盾**：
  匹配 deadline 用"住址淹没时刻−30min"（e-8 西街地势高 +1230min，很宽松，排链靠后合理）；
  名单展示的 latestDeparture 沿实派路线反推（绕行版路线从临江 sh-3 出发穿滨江早淹路段 → 03:35）。
  组合矛盾：前单 e-2 最迟 05:58 出发，链上第 2 单不可能在 03:35 前开始 → **该单按当前路线不可执行**。
  修复方案见下表第 6 项。
- **⑦ 末段所有避难点失效——物理输出诚实，产品预案不合理**：实测 s2024 失效时刻
  sh-4 +840 / sh-2 +960 / sh-3 +1020 / sh-1 +1260（extreme 更早全灭；s30 仅 sh-2 幸存）。
  5.5-7m 涨幅淹没整个河谷建成区不算错，但真实预案必有洪水位以上安置点；且现判定是对
  避难所**坐标单点** pointInFlood，校舍多层建筑的垂直避险未建模。修复见下表第 7 项。
- **⑧ 避难点失效后无重调度——功能 D 覆盖面缺口**：`isFallback` 改派候选只给 sh-3 户
  （e-2/e-4）预生成过；sh-4 在 +840min 失效时派往它的 e-8/e-13/e-16 无任何 fallback，不发生改派。

### 优化项清单（含已确认的实施口径）

| 序 | 项 | 已确认方案 | 优先级 | 工作量 |
|---|---|---|---|---|
| 1 | 人物脸照标记 | 3-5 个剧情户（张奶奶/陈先生/卢阿姨等）用 **AI 生成虚拟人脸**（隐私红线：禁用真实照片）做圆形头像 pin + 保留画像角标，`map.addImage` 位图注册；名单卡与路径卡同步头像，三处一致确认链路；其余户保持画像图标 | P2 | 0.5 天 |
| 2 | 帮扶者/帮扶对象标记重合 | **✔ 已定稿：坐标重合检测 + 固定偏移**——构建 GeoJSON 时检测帮扶者与任一帮扶对象距离 <15m，`icon-offset` 左上错开 + 细引导线；不做聚合 | P2 | 0.5 天 |
| 3 | 部分户名字标签不显示 | **✔ 已定稿：`text-variable-anchor: ['top','bottom','left','right']`** 自动换方位躲碰撞（成因：`text-optional: true` 且未开 text-allow-overlap，密集老城区标签被碰撞剔除）；同时 `symbol-sort-key` 按最迟出发升序保证最紧迫户名字必显 | P1 | 0.5 天 |
| 4 | 时间驱动的撤离状态推演 | 纯前端派生态：每主派任务时间锚点链（计划出发 = latestDeparture − 安全余量 → 接人耗时 → 护送耗时）对比 `currentFrame.clock` 派生 `待命→在途(接人)→护送中→已撤离` 四态，名单/看板/地图图标联动；仅 `dispatch` 档生效（standby/ready 保持静态，分级响应获得真实语义）；**✔ 加分项确认实现：护送中沿路线折线插值移动小点** | P0 | 1.5 天 |
| 5 | sh-3 满员信息透出 | `capacity/occupancy` 回写 `yangshuo_schedule.json`（现容量只活在生成脚本），避难所弹窗显示「容量 2/2 已满」；路径卡串行角标补一句「就近山水园已满员，改派县外语实验中学」 | P1 | 0.25 天 |
| 6 | 时间口径一致性 | 两步走：(a) 先做路线定案后一致性校验——`路线反推最迟出发 < 串行链最早可行开始时刻` 时路径卡/名单红色冲突告警（把矛盾变卖点：系统发现不可行排班）；(b) 正确修法——路线反推 latestDeparture 回灌为新 deadline 重跑 `match_helpers` 一轮（不动点迭代）；另重跑 `gen_access_compare.py` 时加约束「绕行路线不得穿过 +720min 前失效路段」，消除叙事案例与时间窗打架 | P0 | 1 天 |
| 7 | 高地兜底避难点 + 垂直避险 | 西侧高地新增全程安全 sh-5（水深场扫描选点，方法同 sh-3 重选）作为改派/转移终极兜底；校舍类避难所加 `verticalRefuge: true`，场地进水时降级为「仅垂直避险」黄色状态而非红叉 | P1 | 0.5 天 |
| 8-1 | 全量 fallback 改派 | 全部 20 匹配户预生成「次近可用避难所」fallback 路线（离线管线 +约 20 次 Valhalla 调用），前端派生逻辑零改动；改派链按失效时刻级联（sh-4→sh-1→sh-5），依赖第 7 项 | P1 | 0.5 天 |
| 8-2 | 已入住人员二次转移 | 依赖第 4 项状态机（时刻 t 各避难所"已到达"名单）；避难所失效时批量生成「避难所→下一避难所」转移路线，按所内最弱画像水深阈值判可行 | P2 | 1 天 |

### 实施顺序（依赖关系决定）

1. **P0**：第 6 项（口径一致性，先做 (a) 告警再做 (b) 迭代重匹配）→ 第 4 项（时间驱动状态机 + 移动小点）
2. **P1**：第 7 项（sh-5 高地点 + 垂直避险，是 8-1 的前置）→ 第 8-1 项（全量 fallback）→ 第 3 项（标签 variable-anchor）→ 第 5 项（容量透出）
3. **P2**：第 1 项（AI 头像）→ 第 2 项（重合偏移）→ 第 8-2 项（二次转移）

### 实施结论（2026-07-30，全部完成并通过浏览器 e2e 验证）

**逐项状态**：第 6(a)(b) ✅ / 第 4 ✅ / 第 7 ✅ / 第 8-1 ✅ / 第 3 ✅ / 第 5 ✅ / 第 1 ✅ / 第 2 ✅ / 第 8-2 ✅。`npx tsc -b` 零错误。

- **第 6(b) 重匹配迭代结果**：latestDeparture 回灌为新 deadline 重跑 `match_helpers` 后达到不动点，20 条主派 **conflict 全部为 0**（卢阿姨 03:35 矛盾消除）；数据集含 66 条路线、40 条 fallback、4 条 shelterTransfers。
- **第 4 状态机口径**：`buildTimeline` 以 `startMin = max(串行链前单送达, earliestStart, departBy − 120min 安全余量)` 为锚，四态 matched/en_route/escorting/evacuated 仅 dispatch 档随 `currentFrame.minute` 派生；护送小点 `movingDotAt` 沿折线里程占比插值（接人段琥珀 / 护送段绿）。settled 口径：nowMin ≥ 原主派 arriveMin 的户不再触发功能 D 改派，交由第 8-2 整所转移接管。**验证修正（2026-07-30 第十四节复盘）**：当时 e2e 仅确认了小点在单帧出现与名单徽章变化，未覆盖“非出动档零反馈、任务窗口落在两帧间小点不显示、已撤离后地图/倒计时无反馈”等感知问题，呈现层缺陷已由第十四节修复批次闭环。
- **第 7 数据**：sh-5「欧美达书院高地」容量 15 全程安全；verticalRefuge 标记 sh-2/sh-4，失效时黄色降级徽章（上行箭头）而非红叉。容量实测：sh-1 9/8（超员占位提示）、sh-2 8/8、sh-3 2/2 满员、sh-4 1/8、sh-5 0/15。
- **第 8-2 紫色分支说明（非 bug）**：全帧扫描证实三情景中转移路线失效时刻均 ≤ 场地失效时刻（s2024：sh-1 路线 +900 vs 场地 +1260；sh-2 960/960；sh-3 900/1020；sh-4 840/840）——低洼路线先于高处场地被淹，物理合理，故「沿紫色路线整所转移」可行分支在现有情景不出现，演示中呈现的是红色兜底分支「就地垂直避险，请求舰艇/直升机救援」，符合定稿的按所内最弱画像判可行口径。
- **第 1 头像实现**：ImageGen 服务持续 40500 故障，改用 PIL 兜底脚本 `scripts/gen_avatar_placeholders.py` 生成扁平插画风虚拟头像（同样满足禁用真实照片红线），输出 `frontend/public/avatars/{e-1,e-2,e-8,e-16,e-19}.png`；前端 `loadAvatarIcons` canvas 圆形裁剪 + 橙环白边注册为 `avatar-{id}`，缺文件静默回退画像 pin。**服务恢复后直接覆盖同名 png 即可无缝替换**。
- **e2e 验证（浏览器六步全过）**：①初始 24 人名单 ②出动档切换（注意响应级别随时间轴联动回待命，需再点出动）③帧 12 护送中/已撤离多状态 + 绿色移动小点 ④帧 18 sh-2/sh-4 垂直避险徽章 + sh-3 红叉 + 红色整所转移横幅 ⑤山水园 popup「容量 2/2 · 已满员」⑥张奶奶路径卡「⏰ 帮扶者最迟出发 14:37」+ 无冲突 + sh-1 满员占位提示；头像复核截图 `verify_avatar_map.png`。
- **运维备忘**：后端 `/api/schedule` 冷启动需 90-486s（deadline 求解，`@lru_cache(maxsize=4)` 缓存后 0.1s）；**切勿超时重试叠加并发计算**（GIL 争抢会恶化），应重启后单发一次长超时请求预热。

## 十四、时间驱动推演的呈现修复批次（2026-07-30 实测反馈定稿，已完成）

> 来源：用户实测「时间驱动状态推演+移动小点」感知为未实现。复盘结论：**代码存在但
> 触发条件苛刻 + 两个真实缺陷**，第十三节实施结论中对第 4 项的 e2e 验证深度不足。

### 复盘诊断（四个原因叠加）

1. **只在「出动」档生效**：状态机与移动小点均硬判 `alertLevel === 'dispatch'`
   （App.tsx deriveSchedule / MapView escort-dots）。页面加载后为后端初始档位，
   不手动点「出动」则拖时间轴无任何变化，且无 UI 引导——可发现性为零；
2. **人物 pin 设计为不动**：移动的只是独立小圆点（接人琥珀/护送绿），pin 钉在住址，
   与"人被送走图标该走"的直觉不符；
3. **60 分钟帧粒度下小点几乎不可见（真实缺陷）**：多数任务全程 30-70 分钟，
   小点最多出现 1 帧；任务窗口恰好落在两帧之间（如 14:05 出发 14:55 送达）则一帧都不显示；
4. **已撤离后地图零视觉反馈（真实缺陷）**：`evacuated` 只改名单徽章与看板计数，
   地图图标不变灰、不移动、不消失。

**名单倒计时 bug 确认**：`EvacueeList.countdown` 只算 `latestDeparture − simNow`，
不看 matchStatus——人已撤离仍显示「已超 X 时 X 分」，语义荒谬。

### 修复项清单（方案已定稿）

| # | 改动 | 说明 | 状态 |
|---|---|---|---|
| 1 | **已撤离视觉呈现**：`evacuated` 后人物 pin 从住址移到目标避难所（所旁聚簇散开、变灰 + ✓ 角标），住址处不再显示 | 回应"逐步被安排到避难所并在调度板呈现" | ✅ |
| 2 | **护送中呈现**：`escorting/en_route` 时住址 pin 半透明压暗，移动小点放大并带该户姓名标签 | 消除"人在路上但 pin 还在家"割裂感 | ✅ |
| 3 | **小点帧间可见性**：任务窗口落在两帧之间时按帧时刻就近夹取（clamp）保证至少显示一次 | 修 60 分钟帧粒度缺陷 | ✅ |
| 4 | **名单倒计时按状态收敛**：已撤离且所未失效 →「已安置 · XX避难点」不再倒计时；所失效 →「⚠ 避难点失效 · 待转移」；护送中 →「护送中」 | 修 bug，与第 8-2 联动 | ✅ |
| 5 | **出动档引导**：非出动档拖时间轴时「出动」按钮脉冲提示（或 toast「切换出动档查看撤离推演」） | 修可发现性 | ✅ |

**实施顺序**：1 → 4 → 2 → 3 → 5。完成后修正第十三节实施结论中第 4 项的验证描述。

### 实施结论（2026-07-30，全部完成，`npx tsc -b --noEmit` 零错误）

- **第 1 项**：`mapIcons.ts` 新增 `pinDone`（灰底画像 pin + 右上角绿 ✓ 角标）注册为 `evacuee-{profile}-done`；MapView 中已撤离者排除出住址聚簇，坐标改为目标避难所，按所分组用**像素 icon-offset 环形散开**（每环 8 人、半径 34+ring×26px，像素单位保证缩放不聚拢），不显姓名标签避免所旁堆叠，`symbol-sort-key=30` 让位于紧迫户；图例新增「✓ 已安置」。
- **第 2 项**：`en_route/escorting` 时住址 pin `dimmed=true`（icon-opacity 0.35 / text-opacity 0.45 数据驱动）；escort-dots 图层 icon-size 1.2 并加黑字白描边姓名标签（text-allow-overlap 保证必显）。
- **第 3 项**：`routeUtils.ts` 新增 `clampVisible(t, nowMin, nextFrameMin)`：任务窗口 [startMin, arriveMin) 整体落在本帧与下一帧之间时在本帧补显一次（夹取到窗口中点）；`movingDotAt` 与 App `deriveSchedule` 共用同一口径（补显帧同时派生为 escorting），保证地图小点与名单状态一致。
- **第 4 项**：`EvacueeList.statusText` 收敛：evacuated+所正常→绿色「已安置 · XX」；evacuated+所失效→verticalRefuge 所「⚠ 场地进水 · 垂直避险」（琥珀）/ 非垂直所「⚠ 避难点失效 · 待转移」（红）；escorting→「护送中」；其余仍走倒计时。
- **第 5 项**：App 派生 `dispatchHint = alertLevel !== 'dispatch' && frameIndex > 0`；TopBar「出动」按钮 `animate-pulse ring-2 ring-red-400` + 红字提示「← 切「出动」档查看撤离推演」，切入出动档后自动消失。
- **e2e 验证（s2024 情景）**：非出动档拖帧 12 → 脉冲+红字提示出现，切出动后消失 ✅；帧 12 名单：黄阿婆/陈先生「已安置 · 山水园临江避难点」、欧奶奶「护送中」，地图灰✓ pin 聚簇于避难所旁且住址无 pin、移动小点带「欧奶奶」姓名 ✅；帧 14 已撤离 11 人灰✓ 叠簇；帧 18 已撤离 17 人、多所各自成簇，失效所人员名单显「垂直避险/待转移」 ✅。截图见 ComputerUse 临时目录（img-1785392601…/img-1785392863…/img-1785392995….png）。
- **验证环境备忘**：自动化浏览器标签页处于后台 hidden 状态时，MapLibre 暂停渲染且 worker 脚本加载挂起（style 永不完成），截图工具超时——地图类 e2e 必须保证窗口前台可见。

### 概念澄清备忘（回答用户提问，非改动项）

- **分级响应三档**（防"狼来了"误报消耗，见第三节）：预备 standby = 预警初期核对名单
  与匹配，不惊动帮扶者；待命 ready = 通知帮扶者就绪待命；出动 dispatch = 正式发令执行
  撤离。当前代码中 standby/ready 前端表现一致（均静态），仅 dispatch 触发已匹配转在途 +
  时间驱动四态 + 移动小点 + 整所转移，预备/待命的区分暂为通知语义。
- **整所转移的准确边界**：仅覆盖失效避难所内**已入住（evacuated）人员**、且仅非垂直
  避险所（sh-1/sh-3）触发，目标固定为高地兜底所 sh-5（预计算路线）；路线先淹时走红色
  兜底分支（就地垂直避险 + 舟艇救援）。**未入住/在途人员的改派是另一机制**（功能 D
  全量 fallback 级联改派，第 8-1 项）。

## 十五、补充项（2026-07-30 评估确认，基于现有代码核实）

> 本节为第一期收尾的两个候选增补项，已对照真实代码评估合理性。结论：倒推链优先做（低风险、数据已就绪），CV 只做"半真闭环"。

### 1. 时间倒推链可解释 ✅ 优先做（实际 <0.5 天）
**核实结论**：核心数据与算法已完整存在于 `backend/app/core/departure.py` 的 `latest_departure_minutes`——反向递推 `latest = min(latest, fail_i) - dur_i` 已逐段算出"哪一段、几点失效、扣多少护送耗时"，只是中间量被丢弃、未透出。因此这不是新功能，而是"把已有计算暴露出来"。

实施要点：
- **后端**：在 `latest_departure_minutes` 循环内保留每步 `(段序号, 该段失效时刻 fail_i, 该段耗时 dur_i, 递推后 latest)`，组装为 `deriveSteps` 数组随 `Assignment` 透传（约 10 行，零新算法）
- **前端**：点击名单"最迟出发时间"→ 展开 `deriveSteps` 渲染为倒推时间线气泡（复用 `routeUtils.ts` 已导出的 `routeFailureMinute` 与三态着色）
- **串行链户（sequence≥2）**：倒推需叠加"前序户送达时刻"约束，展示时标注该约束（该值 `matching.py` 已算过，一并透出）
- **价值**：正中第三节"决定不走"的核心数字，被追问"14:48 怎么算出来的"时可逐段指屏幕，把黑箱变白箱

### 2. CV 补标签最小闭环 ⚠️ 只做"半真闭环"（0.5-1 天），不碰全自动管线
**核实结论**：`scripts/gen_access_compare.py` 已把"识别→注入→路径改变"闭环的**注入端与路径改变端 100% 做完**（障碍点、Valhalla 绕行实算、⚠️ 标记、绕行倍率对比卡均已跑通并浏览器验证），注释里障碍点叙事本就命名为"CV 街景识别台阶/陡坎"。唯一缺的是最前面"从真实街景图识别台阶"这一下。现有障碍点是人工按 `BARRIER_FRACTIONS` 试出坐标的。

实施建议：
- **做半真闭环**：预选 3-5 张阳朔街景图 + 一次多模态大模型调用（如 Qwen-VL/GPT-4V 识别"是否有台阶/无障碍障碍"，输出判定）+ 坐标**人工预绑定**（回避相机位姿估计深坑）。演示效果："照片 → AI 现场判定'有台阶，轮椅不可通行' → 地图该点亮 ⚠️ → 路径自动绕行"——本质是给已完成的对比开关配一个可信的 AI 输入前端
- **不做全自动管线**：街景抓取（国内街景 GCJ-02 坐标 + 无官方 API）、相机位姿估计、批量识别，投入产出比在赛程尺度下不划算
- **AI 含量定位**：项目已有两个 LLM 落地（简报 + 语音路书），AI 达标线已过，CV 是第三个、锦上添花而非雪中送炭。时间紧张时可只在 PPT 用流程图带过
- **pitch 口径**："CV 识别障碍已打通单点闭环，批量街景接入为工程化后续"——与项目一贯"可插拔/诚实标注"叙事一致

### 排序与取舍
先做倒推链（半天内稳拿），再按时间余量决定 CV 做到什么程度；若赛前吃紧，CV 让位给“演示可靠性”收尾项（数据源指示灯、录屏兜底），后者是当天不翻车的保险，优先级高于第三个 AI 功能。

### 实施结论（2026-07-30 两项均已完成，tsc 零错误 + 接口验证 + 浏览器 e2e 通过）

**第 1 项 倒推链可解释 ✅**
- 后端：`departure.py` 新增 `latest_departure_steps()`（返回 `(latest, steps)`，每步含 segIndex/phase(pickup|escort, 按 `pickup_end_index` 标注)/failMinute/durationMin/latestAfter/clamped），`latest_departure_minutes` 改为委托包装；`schemas.py` 新增 `DeriveStep`，`Assignment.derive_steps` 透传；`mock_data.py` 组装时传 `pickup_end_index=split_idx`。无可行时间窗时 steps 仍完整保留（latestAfter 可为负）。
- 前端：新建 `DeriveChain.tsx`，名单行点击倒计时文字（虚线下划线 + ▾，span+stopPropagation 避免嵌套 button）展开浅蓝气泡：推演期末初始上限 → ⚠ 途中路段 HH:MM 起不可通行→收紧 → 🚶护送段/🏃接人段聚合条（连续可通行小段合并）→ ⏱ 帮扶者最迟出发 + 🏠 户最迟离家；串行链（sequence≥2）标注 earliestStart 约束，conflict 红色告警；未匹配户前端扫帧展示“住址约 HH:MM 达危险阈值 − 30 分钟缓冲”兜底解释。`routeUtils.ts` 新增导出 `simLabel()`。
- e2e 验证数值自洽：黄阿婆气泡链末“户最迟离家 04:57”与行内“剩 4 时 57 分”一致；欧奶奶（轮椅长路线）含 3 次收紧；未匹配户兜底文案与后端 `NO_ROUTE_BUFFER_MIN` 口径一致。

**第 2 项 CV 半真闭环 ✅**
- 素材：ImageGen 服务仍 40500 故障（同第十三节头像），`scripts/gen_streetview_placeholders.py` 用 PIL 画扁平插画风街景兜底（喀斯特峰林+白墙黛瓦+障碍物），输出 `frontend/public/streetview/{e-8,e-14,e-19}.png`，服务恢复后覆盖同名文件即可替换。
- 后端：新建 `services/cv_access.py`（与 briefing 同模式：有 `DASHSCOPE_API_KEY` 时 Qwen-VL 多模态逐图判定，base64 内联；否则模板判定兜底，`source: llm|template`）；`routes.py` 新增 `GET /api/access-scan`；`schemas.py` 新增 `AccessScanItem/AccessScanResponse`。
- 前端：无障碍对比卡片尾部新增「📷 街景 AI 识别」按钮 → 1.2s 识别动画 → 3 张街景图 + 判定文案 + 来源徽章 + “已注入路网 → 绕行 x倍率”，点击定位该户双路线；后端离线时 `mock/data.ts mockAccessScan` 本地模板兜底（文案与后端 `TEMPLATE_VERDICTS` 同口径）。
- 验证：`/api/access-scan` 返回 source=template 三案例判定；浏览器 e2e 确认动画→判定→定位→绕行双线全链路，3 图无裂图。

**其它**：`frontend/src/mock/scheduleDataset.json` 已重新序列化（含 deriveSteps，66 条 assignment），离线/在线逐字段一致。已知小疵（不影响演示）：窄视口（<1108px）下地图 fitBounds 静默失败 + 图例遮挡，宽屏正常。
