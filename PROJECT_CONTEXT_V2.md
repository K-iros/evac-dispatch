# 项目背景与决策交接文档（PROJECT_CONTEXT_V2）——第二期

> 第一期（V1）已完成核心产品闭环，完整历程见 `PROJECT_CONTEXT_V1.md`（共十五节）。
> 本文档承接第二期开发，记录 V1 之后的新决策、新功能与迭代方向。
> 项目根目录：`D:\File\waitan`
> 第二期启动：2026-07-30

---

## 零、如何使用本文档
- **V1（`PROJECT_CONTEXT_V1.md`）= 第一期存档**：项目定位、红线、死因链条、竞品格局、技术方案、淹没模型、调度算法、AI 功能、P0/P1/P2 实施记录、补充项评估，全部沉淀在此，作为不可变的历史基线。
- **V2（本文档）= 第二期工作区**：只记录 V1 完成之后的增量。引用 V1 结论时标注"见 V1 第 N 节"，不复制粘贴。
- 章节从"第二期一、"重新编号，避免与 V1 的一~十五节混淆。

## 一、第一期成果快照（截至 2026-07-30，只读基线）
- **产品形态**：面向社区帮扶者/网格员的灾前疏散调度作战板；Web 指挥端为主，微信小程序帮扶者端已冻结、仅留 `accept`/`checkin` 占位接口。
- **演示场景**：广西阳朔县城；榕江 2025 洪灾开场叙事 + 2024-06 漓江洪水情景推演。
- **技术栈**：前端 React 19 + TypeScript + Vite + Tailwind 4 + MapLibre GL 6；后端 FastAPI；路由 Valhalla 公共实例；淹没 landlab OverlandFlow 离线预计算。
- **已闭环能力**：三情景水动力推演（水深+v·d 双判据）→ 最迟出发时间反推 → 贪心+时间窗匹配（含串行链）→ 三段式无障碍路径 → 时间轴播放/拖动 → 补位演练 → 避难所失效自愈改派 → 分级响应看板 → 无路可走清单+CSV → LLM 派单简报 + LLM 语音路书 + 无障碍路径对比开关。
- **部署形态**：作战板已全栈部署至公网 `http://121.41.172.35/`（docker-compose：前端 nginx 静态托管 + `/api` 同源反代后端 FastAPI，`deploy/deploy.sh` 发版）。

## 二、第二期目标
**解决后端冷启动 9 分钟问题**：后端的淹没推演调度计算结果缓存在进程内存里（`@lru_cache`），服务器/容器重启后缓存清空，首次触发某个情景的计算需要约 9 分钟（之后恢复秒开）。第二期采用**方案 A：调度结果落盘缓存**，使重启后秒级恢复全量服务。（见决策 1，已实施）

**手机扫码访问优化**：作战板无移动端形态，扫码直达时界面破碎；采用"扫码改道落地页 + 窄屏止血"组合方案，不做完整响应式改造。（见决策 2，待实施）

其他优化方向另行讨论后再立项。

**规划项（已列入、待立项排期）**：
- **Web 端调度 → 小程序接收，调度中心与个人联动**：指挥端在作战板派单，帮扶者在微信小程序接单（`accept`）、到场打卡（`checkin`），状态回流作战板，形成派单→接单→执行→反馈完整闭环。已有基础：`miniprogram/` 代码冻结保留（列表/地图/详情三页），后端已留 `accept`/`checkin` 占位接口——属**激活而非从零开发**。立项时需评估：小程序审核周期（V1 期因此冻结）、状态回流的后端持久化（当前后端无数据库，调度态势为预计算静态结果）、真机网络配置要求。

## 三、第二期决策记录
> （随开发推进在此追加，格式沿用 V1：决策 → 依据 → 实施要点 → 验证结果）

### 决策 1：后端冷启动 9 分钟问题采用方案 A——调度结果落盘缓存（2026-07-30，已核对后端计算链路）

**问题与根因**（核对 `backend/app/services/mock_data.py`、`app/core/departure.py`、`app/core/flood.py`、`app/main.py`）：
- `build_mock_schedule` 仅有 `@lru_cache` 进程内存缓存，容器/服务器重启即清空，首次触发某情景需重算约 9 分钟。
- 耗时不在洪水模拟（landlab 帧早已离线预计算为 `flood_frames_*.json`，加载毫秒级），而在**最迟出发时间反推**：66 条派单 × 每路线数十小段 × 25 帧 × shapely 逐点对复杂 landlab 多边形求交（`departure._hit`），纯 CPU 密集。
- **关键性质**：输入 100% 静态（`yangshuo_schedule.json` + `flood_frames_{scenario}.json` 均为仓库固定文件），无运行时输入、无随机性——同输入永远同结果，却每次重启重算。

**方案对比**：
| 方案 | 思路 | 重启后首次响应 | 结论 |
|---|---|---|---|
| **A. 结果落盘（采纳）** | 三情景 `ScheduleState` 预计算成 JSON 随仓库/镜像分发，启动直接加载 | 秒级 | 与项目"离线预计算"哲学一致（洪水帧同款模式），风险极低 |
| B. 运行时磁盘缓存 | 首次算完写 volume，重启读盘 | 首次部署仍 9 分钟×3 | 换服务器/清 volume 即回退，不采纳 |
| C. 算法提速（shapely 向量化等） | 9 分钟压到秒~分钟级 | 分钟级 | 1-2 天+数值口径回归风险；本场景无需反复计算，留作赛后"实时求解 SaaS 形态"再做 |

**实施要点**：
- 新增 `scripts/gen_schedule_cache.py`（模式照抄 `run_flood_sim.py`）：对三情景调用 `build_mock_schedule`，`model_dump_json()` 导出 `backend/data/schedule_{s30,s2024,extreme}.json`，**提交进仓库**（本地跑一次约 27 分钟，一劳永逸）。
- `mock_data.py` 加载逻辑：缓存存在且**指纹匹配**（缓存内嵌输入文件 SHA256：数据集 + 对应洪水帧）→ `model_validate_json` 毫秒级返回；否则回落现有实时计算路径并写回缓存。指纹校验杜绝"改了数据集/重跑了模拟但线上还是旧结果"的隐性 bug。
- `lifespan` 预热与 `deploy.sh` 三情景预热循环**不改**——瞬间完成，顺便变成加载自检。
- 计算代码原样保留，倒推链 `deriveSteps` 逐步中间量都在结果里，"白箱"叙事不受损。

**⚠️ 发布纪律（必须遵守）**：本次后端改动**就是缓存方案本身**，发布前必须确保 `gen_schedule_cache.py` 已在本地跑完、三个 `schedule_*.json` 缓存文件已随代码一起提交。缓存带上 → 部署时后端重启**秒级预热完成，整个发布几分钟结束**；缓存没带上 → 后端重建后回落实时计算，预热又是 20 多分钟——**功能不坏，但发布窗口会拖很长**。发布 checklist：① 本地 `python scripts/gen_schedule_cache.py` 跑完三情景；② `git status` 确认三个 `schedule_*.json` 已暂存；③ 与代码同一提交推送；④ 服务器 `deploy.sh` 预热循环应秒级通过，若仍在长时间计算即说明缓存未生效，立即排查。

**联动收益**：容器崩溃 `restart` 拉起后几秒内全量服务可用，重启自愈链路彻底解除警报。

**验证结果（2026-07-31，已实施）**：
- 代码落地：`mock_data.py` 拆分为 `_compute_schedule`（原计算体，原样保留）+ `build_mock_schedule`（缓存感知包装，保留 `@lru_cache`）；新增 `_input_fingerprint`（数据集 + 该情景洪水帧 SHA256，任一文件缺失即禁用缓存）、`_load_schedule_cache`（指纹不符/损坏返回 None）、`_write_schedule_cache`（写失败静默，只丢缓存不影响服务）。缓存文件格式 `{"inputs": {文件名: sha256}, "state": ScheduleState(camelCase)}`。
- `scripts/gen_schedule_cache.py` 已跑完三情景，`backend/data/schedule_{s30,s2024,extreme}.json` 已生成（各约 0.1 MB；本机计算 s30 54s / s2024 70s / extreme 48s，远快于服务器 9 分钟）。`backend/.gitignore` 的 `!data/*.json` 已覆盖，直接可提交。
- 三情景缓存加载均约 0.013s（对比实时计算快 3 个数量级以上）；s2024 缓存结果与 `_compute_schedule` 实时计算 `model_dump` 全量逐字段一致；篡改指纹后正确拒绝加载并回落实时计算路径。
- `scripts/smoke_verify_s2024.py` 回归通过：sh-3 失效 1020min、e-1 张奶奶 14:48、e-2 陈先生 05:58 等关键基线值与既有一致。
- `lifespan` 预热与 `deploy.sh` 未改动，预热自动退化为毫秒级加载自检。

### 决策 2：手机扫码访问优化——扫码改道落地页 + 窄屏止血，不做完整响应式改造（2026-07-31，已核对前端布局代码 + 真机截图定位）

**问题**：手机扫码直达作战板 `http://121.41.172.35/` 时界面破碎（真机截图实证）：
- 中央大白卡遮半屏、文字挤成竖排 → `MapView.tsx` 图例为 `absolute bottom-3 right-3` 固定内容宽（三行横排约 400px），无 max-width/折行控制，窄屏下横向溢出浮到列表上方。
- 顶部"预备/待命/出动"三档按钮叠压、统计 chips 截断 → `TopBar` 无窄屏断点。
- 地图只剩右侧窄条 → `App.tsx` 固定两栏 flex（名单+地图），无响应式布局。
- "无路可走"面板与图例叠压 → 多个 `absolute` 浮层窄屏下重叠。

**取舍依据**：作战板本质是指挥大屏产品，手机操作三情景推演非真实使用场景；完整响应式改造（名单抽屉化、TopBar 折叠、浮层重排）需 1.5-2 天 + 全量回归，投入产出不成比例，**不做**；留待得奖后有真实移动端诉求再立项。

**采纳方案（两件事，合计约半天）**：
1. **扫码路径改道（治本，约 1 小时）**：二维码不直落作战板，改落 `landing/index.html`（落地页响应式、手机体验完好）；落地页上明示"完整作战板请在电脑端打开"。扫码路径体验闭环完整，作战板移动端缺陷不暴露。与"换公网地址重做二维码"合并执行：落地页需部署上服务器（nginx 加一个静态路径），`DEMO_URL` 指向作战板，`gen_qr.py` 重跑指向落地页。
2. **窄屏止血（防御，2-3 小时）**：防评委直接在手机浏览器输网址打开作战板：
   - **图例止血**：加 `max-w-[calc(100vw-24px)]` + `flex-wrap`；小屏默认收起成"图例"小按钮，点开再展开。
   - **小屏横幅**：`<768px` 断点检测，顶部常驻横幅"本作战板为指挥端设计，建议桌面端访问"（可关闭）。
   - 名单抽屉化等真适配**不在本次范围**。

**验收标准**：① 手机扫新二维码 → 落地页正常渲染、可读可滑，引导文案可见；② 手机直输作战板地址 → 无浮层遮半屏，横幅提示可见；③ 桌面端（≥1108px）回归：图例/布局/浮层与现状像素级一致，零影响。

**验证结果（2026-07-31，已实施；视觉截图因自动化浏览器窗口最小化跳过，改以 DOM/断点检查验收）**：
- 前端代码落地（桌面端 ≥768px 行为零影响，均用 `md:` 断点隔离）：
  - `MapView.tsx` 图例加 `max-w-[calc(100vw-24px)]` + `flex-wrap`；`<768px` 默认收起为「图例」小按钮，点开展开、底部「收起图例」，桌面端由 `md:block` 常显。
  - `App.tsx` 顶部新增 `md:hidden` 琥珀色横幅「💻 本作战板为指挥端设计，建议桌面端访问」（✕ 可关闭）。
  - `TopBar.tsx` 加 `overflow-x-auto md:overflow-x-visible`：修复窄屏工具栏（scrollWidth 1109 > 390）横向溢出撑破 body 的问题，改为容器内横滑。
- 扫码改道：`gen_qr.py` 常量 `DEMO_URL`→`QR_URL` 指向 `/intro/`（落地页，docker-compose 已只读挂载），已重跑生成 `qr-demo.png`（396×396 → `http://121.41.172.35/intro/`）；`landing/index.html` 扫码区文案改为「扫码…了解它」+ 加粗「完整作战板请在电脑端打开」，尾部 CTA hint 补「（建议电脑端打开）」。
- 验证：`npx tsc -b --noEmit` 零错误；390px iframe 视口 DOM 检查全部通过（横幅+✕、图例收起/展开不溢出+收起、横幅可关闭、落地页文案与加粗、二维码加载）；`matchMedia('(min-width:768px)')` 断点逻辑符合预期。
- 未做：名单抽屉化等完整响应式适配（按取舍留待赛后）。视觉截图待浏览器窗口可见时补拍。

## 四、第二期开发环境与约定
- **跨目录写入**：本文档实际位于 `D:\File\waitan`（工作区外），编辑经"工作区临时文件 + PowerShell 复制/追加（required_permissions=all）"完成，沿用 V1 期的 workaround。
- **代码修改一律写入 `D:\File\waitan\`**（前端 `frontend/`、后端 `backend/`、脚本 `scripts/`、小程序 `miniprogram/`）。
- **验证基线**：`npx tsc -b --noEmit` 零错误 + `scripts/smoke_verify_s2024.py` 回归通过 + 浏览器端到端截图。
