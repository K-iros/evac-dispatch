# 项目背景与决策交接文档（PROJECT_CONTEXT_V2）——第二期

> 第一期（V1）已完成核心产品闭环，完整历程见 `PROJECT_CONTEXT_V1.md`（共十五节）。
> 本文档承接第二期开发，记录 V1 之后的新决策、新功能与迭代方向。
> 项目根目录：`D:\File\waitan`
> 第二期启动：2026-07-30

---

## 零、如何使用本文档
- **V1（`PROJECT_CONTEXT_V1.md`）= 第一期存档**：项目定位、红线、死因链条、竞品格局、技术方案、淹没模型、调度算法、AI 功能、P0/P1/P2 实施记录、补充项评估，全部沉淀在此，作为不可变的历史基线。
- **V2（本文档）= 第二期工作区**：只记录 V1 完成之后的增量。引用 V1 结论时标注"见 V1 第 N 节"，不复制粘贴。
- 章节从"第二期一、"重新编号，避免与 V1 的一~十五节混淆。

## 一、第一期成果快照（截至 2026-07-30，只读基线）
- **产品形态**：面向社区帮扶者/网格员的灾前疏散调度作战板；Web 指挥端为主，微信小程序帮扶者端已冻结、仅留 `accept`/`checkin` 占位接口。
- **演示场景**：广西阳朔县城；榕江 2025 洪灾开场叙事 + 2024-06 漓江洪水情景推演。
- **技术栈**：前端 React 19 + TypeScript + Vite + Tailwind 4 + MapLibre GL 6；后端 FastAPI；路由 Valhalla 公共实例；淹没 landlab OverlandFlow 离线预计算。
- **已闭环能力**：三情景水动力推演（水深+v·d 双判据）→ 最迟出发时间反推 → 贪心+时间窗匹配（含串行链）→ 三段式无障碍路径 → 时间轴播放/拖动 → 补位演练 → 避难所失效自愈改派 → 分级响应看板 → 无路可走清单+CSV → LLM 派单简报 + LLM 语音路书 + 无障碍路径对比开关。
- **P0/P1 全部完成，P2 完成过半**（D 避难所重分配已完成；CV 补标签、倒推链、演示剧本模式、数据源指示灯等待第二期或收尾）。
- **V1 遗留候选项**（详见 V1 第十五节 / 第十二节）：
  - 时间倒推链可解释（数据已就绪，<0.5 天，建议第二期优先）
  - CV 补标签"半真闭环"（0.5-1 天）
  - 演示可靠性收尾：数据源指示灯、瓦片预热缓存、流程录屏兜底、投影色弱适配、一键演示剧本模式

## 二、第二期目标（2026-07-30 定档）
**第二期定位为「赛前提交冲刺」**：项目形式为线上提交 → 评委评审得奖 → 线下路演。以资深产品经理视角（商业价值 / 产品叙事 / 完成度）总览后确认——第一期技术与产品闭环完成度已达高水位，第二期**不新增重型功能**，聚焦补齐"交付面"缺口，确保作品能被评委触达并具备商业/规模化叙事。

候选方向池中**本期不做**的项（记录以备后续）：小程序帮扶者端激活、实时水情/气象预警 API 接入、调度算法升级为真正的 VRPTW 求解器——均超出赛前冲刺范围，作为路演后的产品化 roadmap。

具体采纳的优化项与排期见第三章决策 1~3。

## 三、第二期决策记录
> （随开发推进在此追加，格式沿用 V1：决策 → 依据 → 实施要点 → 验证结果）

### 决策 1：采纳资深 PM 视角的赛前优化项清单（2026-07-30）

**依据**：线上提交形态下，评委通过链接/扫码/视频触达作品，不会在本地跑源码；路演阶段商业评委必问"谁付费、怎么规模化、怎么证明有用"。当前短板集中在交付面与商业叙事，而非技术。

**采纳项（按优先级）**：
- **P0（能否被看见，不修必挂）**：
  - ① 全站 Demo 入口写死 `localhost:5200`（`landing/index.html` 第 560/571/838/906 行 + 二维码），线上评委无法访问 → **公网部署 + 换二维码**。前端有 mock 兜底，纯静态可跑。
  - ② 提交物形态错配（README 是给开发者的启动命令）→ **补 2-3 分钟剧本化录屏 + README「评委 3 分钟导览」入口**；录屏兼作现场风险兜底。
- **P1（路演决胜）**：
  - ③ 缺买单方/商业模式 → 补一页 BM（付费方=县应急/街道网格；采购形态=SaaS 年费 or 项目制；单价锚点；可复制单元=一县一部署单元）。
  - ④ 缺北极星指标/效果验证 → 把"效果难以归因"升级为"已设计验证路径"（北极星：预警→全部弱势户完成匹配的时长、无路可走户占比下降）。
  - ⑤ 规模化"城市包"叙事 → 讲清新城上线 SOP + 成本（OSM 抓取→DEM 窗口读取→情景过程线→预计算→部署，脚本均已跑通），代码级 `city config` 抽象可选。
- **P2（一致性与可靠性）**：
  - ⑥ 榕江叙事 vs 阳朔演示的城市错位 → 落地页文案更早显性挑明"榕江是动机、阳朔是同类可复现场景"。
  - ⑦ 后端冷启动 90-486s → 线上固化为静态、零冷启动（见决策 3）。
  - ⑧ 移动端/窄视口 `fitBounds` 静默失败 + 图例遮挡 → 修复或"扫码先落引导页/视频"。
  - ⑨ 语音路书依赖评委机器 `speechSynthesis` → 预生成音频兜底，保住"闭眼听 30 秒"高光。

### 决策 2：9 天提交冲刺排期（2026-07-30，赛前充裕）

关键路径：**D1 静态化 → D2 部署 → D7 录屏**（录屏依赖部署稳定 + 叙事定稿）；D9 整天缓冲。

| 阶段 | 日期 | 内容 |
|---|---|---|
| 一·让作品被看见 | D1 | 后端结果静态化固化（部署前置，详见决策 3） |
| | D2 | 公网部署（Vercel/Netlify 纯静态）+ 改 `DEMO_URL` + 重跑 `gen_qr.py` 换二维码 |
| | D3 | 窄视口/移动端修复 + 榕江/阳朔叙事衔接文案 + TTS 音频兜底 |
| 二·商业与规模化叙事 | D4 | 商业模式一页 + 北极星指标与验证设计 |
| | D5 | "城市包"规模化叙事（`city config` 抽象可选） |
| 三·提交物成型与彩排 | D6 | Pitch Deck 整合（复用落地页 30 秒速览/五步计算链/对比骨架） |
| | D7 | 剧本化录屏 + README 评委导览 |
| | D8 | 跨浏览器/跨设备彩排 + 投影色弱适配 + 真机预热预案演练 + 回归 |
| | D9 | 缓冲 + 终稿 + 打包提交（公网链接+视频+PPT+一页 BM+repo） |

**可砍项（时间吃紧时）**：D5 的 `city config` 代码抽象（留叙事即可）、D3 窄视口深修（用"扫码进视频"退路替代）。

### 决策 3：D1 静态化的全面影响评估——后端零影响，但现有 mock 兜底非全保真，须打包真实预计算数据（2026-07-30，已核对前端数据层代码）

**核对依据**：`frontend/src/api/client.ts`（`tryFetch` 失败即回退 mock）、`frontend/src/mock/data.ts`、`frontend/vite.config.ts`。

**结论一：对后端零影响**。
- 静态部署无 `/api` 后端 → 所有 `fetch` 失败自动走 mock 分支（前端既有行为，不改后端）。
- `vite.config.ts` 的 `/api` 代理仅 `npm run dev` 生效，`npm run build` 产物不带代理，与后端彻底解耦。
- `backend/app/**` 原样保留，作为本地开发与路演真机的"完整在线版"。

**结论二：现有 mock 兜底并非「全保真」，直接裸部署会露馅**（关键发现，修正 V1"离线逐字段一致"的宽泛表述）：
- ✅ `mockSchedule()` 返回 `scheduleDataset.json`（= `build_mock_schedule('s2024')` 完整序列化，含 latestDeparture/deriveSteps/66 assignment）——**仅此项全保真**。
- ❌ `mockFloodFrames()` 生成的是**围绕进水点的同心矩形占位**，非后端真实 landlab 多边形（`flood_frames_s2024.json` 11MB 沿漓江河谷物理形态）→ 核心卖点"水动力淹没推演"离线下退化为方块。
- ❌ `mockSchedule()`/`mockFloodFrames()` **均不接收 scenario 参数**，离线下三套情景（30年一遇/2024/极端）永远返回 s2024 → 情景切换失效。
- ⚠️ LLM 简报/语音路书/CV 识别离线回退本地模板（`source: template`）——文案在、可演示，但无 LLM 润色版；录屏若要展示 AI 润色版需连后端录或预生成。

**结论三：D1 的正确做法（Case B，非裸靠现有 mock）**：
- 把 `flood_frames_{s30,s2024,extreme}.json`（约 32MB，按情景懒加载）+ 三套情景 schedule 打包进前端静态资源。
- **仅改数据层**（`api/client.ts` + `mock/data.ts`）：fallback 按 scenario 读对应真实 JSON，替换合成矩形。
- **展示组件零改动**：`MapView`/`EvacueeList`/`TopBar` 消费同一套领域类型，风险收敛在数据层。
- 净效果：后端零影响 + 前端组件零影响 + 展示全保真，并顺手修掉离线兜底的两个真实缺口（淹没矩形 + 情景切换失效）。工作量 0.5-1 天，风险低。

## 四、第二期开发环境与约定
- **跨目录写入**：本文档实际位于 `D:\File\waitan`（工作区外），编辑经"工作区临时文件 + PowerShell 复制/追加（required_permissions=all）"完成，沿用 V1 期的 workaround。
- **代码修改一律写入 `D:\File\waitan\`**（前端 `frontend/`、后端 `backend/`、脚本 `scripts/`、小程序 `miniprogram/`）。
- **验证基线**：`npx tsc -b --noEmit` 零错误 + `scripts/smoke_verify_s2024.py` 回归通过 + 浏览器端到端截图。
